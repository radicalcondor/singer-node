import { sum } from './';

describe('blah', () => {
  it('works', () => {
    expect(sum(1, 1)).toEqual(2);
  });
});

describe('@radicalcondor/singer-node', () => {
  describe('Parsing messages', () => {
    it('should successfully parse a message correctly', () => {});
    it('should successfully parse a message record with a version correctly', () => {});
    it('should fail to parse a message with an invalid date', () => {});
    it('should successfully parse a message with a valid date', () => {});
    it('should fail to parse a message with a missing record', () => {});
    it('should fail to parse a message with a missing stream', () => {});
    it('should fail to parse a message with a missing record', () => {});
    it('should successfully parse a message with a valid schema', () => {});
  });
});
