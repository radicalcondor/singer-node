describe('Errors', () => {
  it('should throw a SingerError with the correct message', () => {});
  it('should throw a SingerError with the correct multiline message', () => {});
  it('should throw a SingerConfigurationError with the correct message', () => {});
  it('should throw a SingerDiscoveryError with the correct message', () => {});
  it('should throw a SingerSyncError with the correct message', () => {});
  it('should throw a SingerRetryableRequestError with the correct message', () => {});
});
